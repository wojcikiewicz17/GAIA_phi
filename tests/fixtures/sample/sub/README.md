# tests/fixtures/sample/sub/ — Subpasta de exclusão de fixture

Esta subpasta contém o arquivo binário `ignore.bin` para validar cenários de filtro por extensão e comportamento de exclusão em varredura.
